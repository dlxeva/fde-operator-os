interface:
  display_name: "Pilot Designer"
  short_description: "Compress a bounded loop into a credible pilot with acceptance and eval"
  default_prompt: "Use pilot-designer to define the minimum viable loop, acceptance contract, eval, and production handoff."
